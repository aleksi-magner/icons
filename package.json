{
  "name": "vi-icons",
  "author": "Aleksi Magner",
  "version": "1.0.13",
  "description": "Lightweight icon library.",
  "license": "MIT",
  "main": "icons.css",
  "scripts": {
    "patch": "npm version patch --force -m 'Upgrade to %s' && git push && git push --tags && npm publish",
    "test": "exit 0"
  }
}
