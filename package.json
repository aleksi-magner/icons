{
  "name": "vi-icons",
  "author": "Aleksey Magner",
  "version": "1.3.1",
  "description": "Lightweight icon library.",
  "license": "MIT",
  "main": "icons.css",
  "scripts": {
    "deploy": "git push && git push --tags && npm publish",
    "patch": "git add . && npm version patch --force -m 'Upgrade to %s' && npm run deploy",
    "minor": "git add . && npm version minor --force -m 'Upgrade to %s' && npm run deploy",
    "major": "git add . && npm version major --force -m 'Upgrade to %s' && npm run deploy",
    "test": "exit 0"
  }
}
